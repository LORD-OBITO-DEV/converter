<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>User Dashboard</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>Bonjour <%= user.username %> 👋</h1>
    <nav>
      <a href="/logout">Déconnexion</a>
    </nav>
  </header>

  <main>
    <section>
      <h2>Mon compte</h2>
      <ul>
        <li>Email : <%= user.email %></li>
        <li>Solde actuel : <%= user.balance || 0 %> €</li>
      </ul>
    </section>

    <section>
      <h2>Mes numéros achetés</h2>
      <% if (boughtNumbers.length === 0) { %>
        <p>Vous n'avez encore acheté aucun numéro.</p>
      <% } else { %>
        <table border="1">
          <tr>
            <th>Numéro</th>
            <th>Date d’achat</th>
            <th>Status</th>
          </tr>
          <% boughtNumbers.forEach(num => { %>
            <tr>
              <td><%= num.phoneNumber %></td>
              <td><%= num.date %></td>
              <td><%= num.active ? "Actif" : "Inactif" %></td>
            </tr>
          <% }) %>
        </table>
      <% } %>
    </section>

    <section>
      <h2>Actions</h2>
      <form method="POST" action="/payment/add-credit">
        <label>Recharger mon compte :</label>
        <input type="number" name="amount" min="1" placeholder="Montant en €" required>
        <button type="submit">Ajouter du crédit</button>
      </form>
    </section>
  </main>

  <footer>
    <p>&copy; 2025 Lord Obito | Tous droits réservés</p>
  </footer>
</body>
</html>
